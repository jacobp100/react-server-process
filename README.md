#react-server-process

Demonstrates how to have React components live and live-update on the server, and serve them as HTML. Differs from `react-dom-server` in that the component is actually mounted.
